version: '3.8'

services:
  thecampaign:
    build:
      context: .
      dockerfile: dockerfile
      args:
        VERSION: ${VERSION:-dev}
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    container_name: thecampaign
    restart: unless-stopped
